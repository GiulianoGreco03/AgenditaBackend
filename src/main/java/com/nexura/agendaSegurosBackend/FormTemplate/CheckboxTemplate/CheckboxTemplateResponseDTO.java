package com.nexura.agendaSegurosBackend.FormTemplate.CheckboxTemplate;

public record CheckboxTemplateResponseDTO(Long id,
                                          String title,
                                          int order) {
}
